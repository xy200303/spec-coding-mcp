# 为 spec 工作流增加下一步推荐

## Meta

- status: done
- source: user-prompt

## 用户原始描述

继续优化 MCP 工具体验：在 spec_list 和 spec_context 没有可执行任务或列出状态时，增加清晰的下一步推荐，告诉模型应该调用 spec_bootstrap、spec_create、spec_todo、spec_context、spec_checkpoint 或 spec_done 中的哪一个。目标是减少模型在工具选择上的猜测，让工作流更自动、更稳定。更新渲染逻辑、上下文说明、README 和测试。

## TODO

- [x] 继续优化 MCP 工具体验：在 spec_list 和 spec_context 没有可执行任务或列出状态时，增加清晰的下一步推荐，告诉模型应该调用 spec_bootstrap、spec_create、spec_todo、spec_context、spec_checkpoint 或 spec_done 中的哪一个。目标是减少模型在工具选择上的猜测，让工作流更自动、更稳定。更新渲染逻辑、上下文说明、README 和测试。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T05:30:14.762Z
- summary: 为 spec_context 和 spec_list 增加共享的 Recommended Next Step 推荐，减少模型选择工具时的猜测。

### Summary

- 为 spec_context 和 spec_list 增加共享的 Recommended Next Step 推荐，减少模型选择工具时的猜测。

### Completed TODOs

- 继续优化 MCP 工具体验：在 spec_list 和 spec_context 没有可执行任务或列出状态时，增加清晰的下一步推荐，告诉模型应该调用 spec_bootstrap、spec_create、spec_todo、spec_context、spec_checkpoint 或 spec_done 中的哪一个。目标是减少模型在工具选择上的猜测，让工作流更自动、更稳定。更新渲染逻辑、上下文说明、README 和测试。

### Changed Files

- `src/spec/workflow-next-step.ts`
- `src/spec/context-markdown.ts`
- `src/spec/context.ts`
- `src/mcp/register-read-tools.ts`
- `README.md`
- `test/smoke.ts`

### Verification

- passed `bun run build`：TypeScript build passed.
- passed `bun test/smoke.ts`：Smoke test passed; Bun printed sandbox EPERM warning after success.
- passed `bun test/unit.ts`：Unit tests passed; Bun printed sandbox EPERM warning after success.
- passed `bun run release:check`：Release checks passed; Bun printed sandbox EPERM warning after success.
- passed `git diff --check`：No whitespace errors; Git reported LF-to-CRLF warnings only.

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 空工作流推荐 | 没有 selected spec、open TODO、active、todo、review | Recommended Next Step 推荐调用 spec_bootstrap，并提醒不要直接实现代码 | projectKind 选择由用户或 auto 决定 | 避免模型在无任务上下文时直接写代码 | bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`src/spec/context-markdown.ts`<br>`test/smoke.ts` |
| TODO 工作流推荐 | 存在 open TODO | 推荐按 open TODO 顺序执行，并在完成一个 TODO 后调用 spec_checkpoint | spec_context 和 spec_list 共用同一推荐规则 | 不会推荐 spec_done 处理未完成 TODO | bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`src/mcp/register-read-tools.ts` |
| active/review 工作流推荐 | 存在 active/selected spec 或 review spec | active 推荐实现后 checkpoint/done；review 推荐先阅读源码并补全真实行为，再转 active 或创建 active spec | 推荐只基于 spec 状态，不推断业务内容 | review 不会被当作可直接实现的业务事实 | bun run build | `src/spec/workflow-next-step.ts` |
| 文档说明 | 用户阅读 README 工具说明 | 说明 spec_context 和 spec_list 会输出 Recommended Next Step，模型应优先按推荐选择下一步工具 | README 保持主路径工具分组 | 减少模型凭旧对话记忆猜工具 | git diff --check | `README.md` |

### Risks

- 推荐逻辑是启发式状态判断，不替代用户明确指令；用户明确指定工具时仍应尊重用户。

### Blockers

- 无

## Done

- doneAt: 2026-06-20T05:30:26.344Z
- note: 已为 spec_context/spec_list 增加下一步推荐。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 下一步工具推荐 | 调用 spec_context 或 spec_list 查看当前工作流状态 | 输出 Recommended Next Step，基于当前状态推荐 spec_bootstrap、spec_checkpoint、spec_create/spec_todo、active 实现或 review 补全流程 | 没有任务时推荐 spec_bootstrap；有 open TODO 时推荐执行 TODO 并 checkpoint；有 active/selected spec 时推荐实现并记录结果；只有 review 时推荐先补全源码审查 | 推荐不替代用户明确指令，也不把源码线索当业务事实 | bun run build; bun test/smoke.ts; bun test/unit.ts; bun run release:check; git diff --check | `src/spec/workflow-next-step.ts`<br>`src/spec/context-markdown.ts`<br>`src/spec/context.ts`<br>`src/mcp/register-read-tools.ts`<br>`README.md`<br>`test/smoke.ts` |
