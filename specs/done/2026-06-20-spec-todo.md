# 优化 spec_todo 任务提取质量

## Meta

- status: done
- source: user-prompt

## 用户原始描述

优化 spec_todo 从用户 prompt 生成 TODO 列表的质量，避免把段落标题、验收标题和纯说明文字都当成可执行任务。

目标：
- spec_todo 应优先识别真正可执行的任务行。
- 像 `目标：`、`验收：`、`实际行为记录要求：` 这样的结构标题不应生成 checkbox TODO。
- 验证命令可以保留在说明或验收区域，但不要变成需要实现的 TODO 项。
- 如果用户 prompt 已经包含 markdown checkbox，则保持 checkbox 语义。
- 保持简单规则，不引入复杂 NLP 或大依赖。

验收：
- 新增或更新单测覆盖标题过滤、验证命令过滤、已有 checkbox 保留。
- `bun run build` 通过。
- `bun test/unit.ts` 通过。
- `bun test/smoke.ts` 通过。
- `bun run release:check` 通过。
- `git diff --check` 通过。

## TODO

- [x] 优化 spec_todo 从用户 prompt 生成 TODO 列表的质量，避免把段落标题、验收标题和纯说明文字都当成可执行任务。
- [x] 目标：
- [x] spec_todo 应优先识别真正可执行的任务行。
- [x] 像 `目标：`、`验收：`、`实际行为记录要求：` 这样的结构标题不应生成 checkbox TODO。
- [x] 验证命令可以保留在说明或验收区域，但不要变成需要实现的 TODO 项。
- [x] 如果用户 prompt 已经包含 markdown checkbox，则保持 checkbox 语义。
- [x] 保持简单规则，不引入复杂 NLP 或大依赖。
- [x] 验收：
- [x] 新增或更新单测覆盖标题过滤、验证命令过滤、已有 checkbox 保留。
- [x] `bun run build` 通过。
- [x] `bun test/unit.ts` 通过。
- [x] `bun test/smoke.ts` 通过。
- [x] `bun run release:check` 通过。
- [x] `git diff --check` 通过。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T15:33:28.898Z
- summary: 优化 spec_todo 的 prompt-to-TODO 提取规则：过滤结构标题和纯验证命令，保留真正可执行任务，并保持用户已有 markdown checkbox 的完成状态。

### Summary

- 优化 spec_todo 的 prompt-to-TODO 提取规则：过滤结构标题和纯验证命令，保留真正可执行任务，并保持用户已有 markdown checkbox 的完成状态。

### Completed TODOs

- 优化 spec_todo 从用户 prompt 生成 TODO 列表的质量，避免把段落标题、验收标题和纯说明文字都当成可执行任务。
- 目标：
- spec_todo 应优先识别真正可执行的任务行。
- 像 `目标：`、`验收：`、`实际行为记录要求：` 这样的结构标题不应生成 checkbox TODO。
- 验证命令可以保留在说明或验收区域，但不要变成需要实现的 TODO 项。
- 如果用户 prompt 已经包含 markdown checkbox，则保持 checkbox 语义。
- 保持简单规则，不引入复杂 NLP 或大依赖。
- 验收：
- 新增或更新单测覆盖标题过滤、验证命令过滤、已有 checkbox 保留。
- `bun run build` 通过。
- `bun test/unit.ts` 通过。
- `bun test/smoke.ts` 通过。
- `bun run release:check` 通过。
- `git diff --check` 通过。

### Changed Files

- `src/templates/prompt-documents.ts`
- `test/unit.ts`
- `test/smoke.ts`

### Verification

- passed `bun run build`：TypeScript 编译通过。
- passed `bun test/unit.ts`：覆盖标题过滤、验证命令过滤、已有 checkbox 状态保留。
- passed `bun test/smoke.ts`：覆盖 createTodoFromPrompt 真实写文件输出。
- passed `bun run release:check`：发布契约未受影响。
- passed `git diff --check`：仅出现 Windows LF/CRLF 提示，无空白错误。

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 结构标题过滤 | 用户 prompt 中存在 `目标：`、`验收：`、`实际行为记录要求：` 等说明性标题 | 这些标题保留在用户原始描述中，但不会生成 `- [ ]` TODO。 | 内置标题集合采用简单显式规则，不依赖 NLP。 | 如果 prompt 存在 markdown bullet，则优先从 bullet 行提取任务，避免段落说明误入 TODO。 | bun test/unit.ts; bun test/smoke.ts | `src/templates/prompt-documents.ts`<br>`test/unit.ts`<br>`test/smoke.ts` |
| 验证命令过滤 | 用户 prompt 中存在 `bun run build`、`git diff --check` 等纯验证命令行 | 验证命令保留在原始描述中，但不会变成模型需要实现的 TODO 项。 | 命令过滤支持常见 bun/npm/pnpm/yarn/node/git 命令。 | 只过滤纯命令验收行，避免吞掉带业务动作描述的任务。 | bun test/unit.ts; bun test/smoke.ts | `src/templates/prompt-documents.ts`<br>`test/unit.ts`<br>`test/smoke.ts` |
| 用户 checkbox 保留 | 用户 prompt 已经包含 `- [x]` 或 `- [ ]` markdown checkbox | 生成的 TODO 保留原始完成状态，`[x]` 仍为已完成，`[ ]` 仍为未完成。 | 普通 bullet 生成未完成 TODO。 | 当 prompt 没有任何 bullet 时，仍从非空普通行提取任务，保持简单临时任务可用。 | bun test/unit.ts; bun test/smoke.ts | `src/templates/prompt-documents.ts`<br>`test/unit.ts`<br>`test/smoke.ts` |

### Risks

- 无

### Blockers

- 无

## Done

- doneAt: 2026-06-20T15:33:40.673Z
- note: 已完成 spec_todo 任务提取质量优化，减少模型执行噪音。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 结构标题过滤 | prompt 包含说明性结构标题 | 标题不再生成 checkbox TODO，只保留在用户原始描述中。 | 显式标题集合过滤，不引入依赖。 | markdown bullet 存在时优先使用 bullet 行作为任务来源。 | bun test/unit.ts; bun test/smoke.ts | `src/templates/prompt-documents.ts` |
| 验证命令过滤 | prompt 包含纯验证命令行 | 验证命令不再生成可执行 TODO，避免模型把测试命令当功能实现任务。 | 支持常见 bun/npm/pnpm/yarn/node/git 命令。 | 非纯命令的业务任务不会被过滤。 | bun test/unit.ts; bun test/smoke.ts | `src/templates/prompt-documents.ts` |
| checkbox 状态保留 | prompt 已包含 `- [x]` 或 `- [ ]` | 生成 TODO 时保留用户原始勾选状态。 | 普通 bullet 默认生成未完成 TODO。 | 没有 bullet 时继续从普通非空行提取任务。 | bun test/unit.ts; bun test/smoke.ts | `src/templates/prompt-documents.ts` |
