# 拆分 CLI init 子命令

## Meta

- status: done
- source: user-prompt

## 用户原始描述

继续重构 CLI 代码组织：把 src/cli/main.ts 中的 init 交互注册流程拆到 src/cli/command-init.ts，main.ts 只保留顶层 help/version/serve/status/bootstrap/init 分发。保持现有 init 行为、输出和注册逻辑不变；新文件必须有顶部文件注释；不引入新依赖；保持 build、smoke、unit、release-check 通过。

## TODO

- [x] 继续重构 CLI 代码组织：把 src/cli/main.ts 中的 init 交互注册流程拆到 src/cli/command-init.ts，main.ts 只保留顶层 help/version/serve/status/bootstrap/init 分发。保持现有 init 行为、输出和注册逻辑不变；新文件必须有顶部文件注释；不引入新依赖；保持 build、smoke、unit、release-check 通过。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T15:22:58.846Z
- summary: 将 CLI init 交互注册流程拆到 command-init.ts，让 main.ts 只负责顶层命令分发。

### Summary

- 将 CLI init 交互注册流程拆到 command-init.ts，让 main.ts 只负责顶层命令分发。

### Completed TODOs

- 继续重构 CLI 代码组织：把 src/cli/main.ts 中的 init 交互注册流程拆到 src/cli/command-init.ts，main.ts 只保留顶层 help/version/serve/status/bootstrap/init 分发。保持现有 init 行为、输出和注册逻辑不变；新文件必须有顶部文件注释；不引入新依赖；保持 build、smoke、unit、release-check 通过。

### Changed Files

- `src/cli/main.ts`
- `src/cli/command-init.ts`

### Verification

- passed `bun run build`：TypeScript 构建通过。
- passed `bun test/smoke.ts`：CLI 和注册相关 smoke 流程通过。
- passed `bun test/unit.ts`：单元测试通过。
- passed `bun run release:check`：发布契约检查通过。
- passed `node dist/index.js --help`：顶层 help 输出保持。
- passed `node dist/index.js status --project-root . --json`：其它子命令拆分后仍正常。
- passed `git diff --check`：无 whitespace error；仅有 Git LF/CRLF 提示。

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| init 子命令拆分 | 运行 specc init | runCli 分发到 command-init.ts 中的 runInit，交互扫描、选择工具、注册和结果输出逻辑保持不变。 | 仍扫描 Codex、Claude Code、OpenCode、Cursor、Continue 和 Windsurf。 | 用户取消 multiselect 时仍调用 cancel("Init cancelled") 并返回。 | bun test/smoke.ts | `src/cli/command-init.ts`<br>`src/cli/main.ts` |
| main 顶层分发 | 运行 specc、specc --version、specc serve、specc status、specc bootstrap、specc init | main.ts 只负责顶层 help/version/serve/status/bootstrap/init 分发。 | 裸 specc 继续显示 help。 | 未知顶层命令仍抛 Unknown command。 | node dist/index.js --help; bun test/smoke.ts | `src/cli/main.ts` |
| 结构质量 | 查看 src/cli 文件组织 | init、bootstrap、status 和 option parsing 分别有独立文件，且新增文件有顶部文件注释。 | 不引入新的命令行框架或依赖。 | registry write/detect 逻辑仍留在原 registry 模块。 | bun run build | `src/cli/command-init.ts`<br>`src/cli/command-bootstrap.ts`<br>`src/cli/command-status.ts`<br>`src/cli/cli-options.ts` |

### Risks

- 无

### Blockers

- 无

## Done

- doneAt: 2026-06-20T15:23:16.925Z
- note: CLI init command moved to its own command module without behavior changes.

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| init 子命令拆分 | 运行 specc init | runCli 分发到 command-init.ts 中的 runInit，交互扫描、选择工具、注册和结果输出逻辑保持不变。 | 仍扫描 Codex、Claude Code、OpenCode、Cursor、Continue 和 Windsurf。 | 用户取消 multiselect 时仍调用 cancel("Init cancelled") 并返回。 | bun test/smoke.ts | `src/cli/command-init.ts`<br>`src/cli/main.ts` |
| main 顶层分发 | 运行 specc、specc --version、specc serve、specc status、specc bootstrap、specc init | main.ts 只负责顶层 help/version/serve/status/bootstrap/init 分发。 | 裸 specc 继续显示 help。 | 未知顶层命令仍抛 Unknown command。 | node dist/index.js --help; bun test/smoke.ts | `src/cli/main.ts` |
| 结构质量 | 查看 src/cli 文件组织 | init、bootstrap、status 和 option parsing 分别有独立文件，且新增文件有顶部文件注释。 | 不引入新的命令行框架或依赖。 | registry write/detect 逻辑仍留在原 registry 模块。 | bun run build | `src/cli/command-init.ts`<br>`src/cli/command-bootstrap.ts`<br>`src/cli/command-status.ts`<br>`src/cli/cli-options.ts` |
