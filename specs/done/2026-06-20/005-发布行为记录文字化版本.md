# 发布行为记录文字化版本

## Meta

- status: done
- source: user-prompt

## 用户原始描述

发布 behaviorRecords 文字化渲染改动。

要求：
- 确认 npm latest 和本地版本，选择下一个 patch 版本。
- 升级 package.json 和 package-lock.json。
- 运行发布前验证：bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。
- 提交版本升级，推送 main。
- 创建并推送对应 tag，触发 GitHub Actions npm 发布。
- 确认 GitHub Actions 和 npm latest 结果，记录发布行为。

## TODO

- [x] 确认 npm latest 和本地版本，选择下一个 patch 版本。
- [x] 升级 package.json 和 package-lock.json。
- [x] 运行发布前验证：bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。
- [x] 提交版本升级，推送 main。
- [x] 创建并推送对应 tag，触发 GitHub Actions npm 发布。
- [x] 确认 GitHub Actions 和 npm latest 结果，记录发布行为。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## Done

- doneAt: 2026-06-20T18:40:12.210Z
- note: 0.2.9 已通过 GitHub Actions 发布到 npm。

## 最终行为契约

1. 0.2.9 发布完成
  - 条件：behaviorRecords 文字化渲染改动已提交，package 版本升级到 0.2.9
  - 结果：main 和 v0.2.9 tag 已推送；GitHub Actions Publish npm 成功；npm latest 为 0.2.9。
  - 默认行为：发布仍采用手动版本 bump + tag push 触发 GitHub Actions 的流程。
  - 边界处理：npm registry 在 workflow 完成后从 0.2.8 更新到 0.2.9。
  - 验证：bun run build; bun test/unit.ts; bun test/smoke.ts; bun run release:check; git diff --check; npm view @dev_xiaoyun/spec-coding-mcp version
  - 关联文件：
    - `package.json`
    - `package-lock.json`
    - `src/spec/behavior-record.ts`
    - `test/unit.ts`
    - `test/smoke.ts`
