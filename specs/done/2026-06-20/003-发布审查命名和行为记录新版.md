# 发布审查命名和行为记录新版

## Meta

- status: done
- source: user-prompt

## 用户原始描述

发布当前审查友好的 spec 命名、真实行为记录和 MCP 工具声明精简改动。

要求：
- 确认当前 npm 最新版本和本地版本。
- 将 package.json 和 package-lock.json 版本升级到下一个 patch 版本。
- 运行发布前验证：bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。
- 提交本次代码和 spec 记录，commit message 使用中文。
- 创建对应 vX.Y.Z tag 并推送 main 和 tag，触发 GitHub Actions npm 发布。
- 记录发布结果、关联文件、验证命令和风险。

## TODO

- [x] 确认当前 npm 最新版本和本地版本。
- [x] 将 package.json 和 package-lock.json 版本升级到下一个 patch 版本。
- [x] 运行发布前验证：bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。
- [x] 提交本次代码和 spec 记录，commit message 使用中文。
- [x] 创建对应 vX.Y.Z tag 并推送 main 和 tag，触发 GitHub Actions npm 发布。
- [x] 记录发布结果、关联文件、验证命令和风险。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## Done

- doneAt: 2026-06-20T18:29:16.549Z
- note: 0.2.8 已通过 GitHub Actions 发布到 npm。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 版本发布 | package.json/package-lock.json 升级到 0.2.8 且发布前验证通过 | 提交 42f8ba2 创建成功，v0.2.8 tag 已推送，npm latest 更新为 0.2.8。 | 继续采用手动版本 bump + tag push 触发 GitHub Actions 发布的方式。 | 本地未安装 gh，使用 GitHub REST API 查询 CI；npm view 和 registry latest 均确认 0.2.8。 | bun run build; bun test/unit.ts; bun test/smoke.ts; bun run release:check; git diff --check; npm view @dev_xiaoyun/spec-coding-mcp version | `package.json`<br>`package-lock.json`<br>`.github/workflows/publish-npm.yml` |
| CI 发布结果 | origin/main 和 v0.2.8 tag 推送后 | GitHub Actions CI run 27880020851 success，Publish npm run 27880021664 success。 | tag push 触发 publish-npm.yml；main push 触发 CI。 | npm registry latest 从 0.2.7 更新为 0.2.8。 | GitHub Actions REST API; npm registry latest API | `.github/workflows/ci.yml`<br>`.github/workflows/publish-npm.yml` |
