# 统一 spec 工作流入口和自动初始化

## Meta

- status: done
- source: user-prompt

## 用户原始描述

优化 Spec Coding MCP 的工作流体验：当前模型有时创建 TODO、有时直接 done、有时没有使用 spec；新项目和旧项目也不会自然生成 spec 文档。需要增加统一的项目启动/引导入口，明确新项目、旧项目、临时任务、功能开发、完成归档的状态流转规则。工具应指导模型：新项目自动生成 specs 基础文档、AGENTS 和起步 spec；旧项目自动生成 AI 源码审查任务；小任务用 TODO；功能开发用 active spec；只有完成验证后才能 done。更新 MCP 工具、模板/说明和测试，让流程更专业、一致、可预期。

## TODO

- [x] 优化 Spec Coding MCP 的工作流体验：当前模型有时创建 TODO、有时直接 done、有时没有使用 spec；新项目和旧项目也不会自然生成 spec 文档。需要增加统一的项目启动/引导入口，明确新项目、旧项目、临时任务、功能开发、完成归档的状态流转规则。工具应指导模型：新项目自动生成 specs 基础文档、AGENTS 和起步 spec；旧项目自动生成 AI 源码审查任务；小任务用 TODO；功能开发用 active spec；只有完成验证后才能 done。更新 MCP 工具、模板/说明和测试，让流程更专业、一致、可预期。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T04:57:56.553Z
- summary: 新增统一入口 spec_bootstrap，并收紧新项目、旧项目、TODO、active、checkpoint、done 的流程规则。

### Summary

- 新增统一入口 spec_bootstrap，并收紧新项目、旧项目、TODO、active、checkpoint、done 的流程规则。

### Completed TODOs

- 优化 Spec Coding MCP 的工作流体验：当前模型有时创建 TODO、有时直接 done、有时没有使用 spec；新项目和旧项目也不会自然生成 spec 文档。需要增加统一的项目启动/引导入口，明确新项目、旧项目、临时任务、功能开发、完成归档的状态流转规则。工具应指导模型：新项目自动生成 specs 基础文档、AGENTS 和起步 spec；旧项目自动生成 AI 源码审查任务；小任务用 TODO；功能开发用 active spec；只有完成验证后才能 done。更新 MCP 工具、模板/说明和测试，让流程更专业、一致、可预期。

### Changed Files

- `src/spec/scaffold.ts`
- `src/mcp/register-read-tools.ts`
- `src/templates/prompt-protocol.ts`
- `src/templates/agents.ts`
- `src/spec/checkpoint-writer.ts`
- `README.md`
- `test/smoke.ts`

### Verification

- passed `bun run build`：TypeScript build passed.
- passed `bun test/smoke.ts`：Smoke test passed; Bun printed sandbox EPERM warning after success.
- passed `bun test/unit.ts`：Unit tests passed; Bun printed sandbox EPERM warning after success.
- passed `bun run release:check`：Release checks passed; Bun printed sandbox EPERM warning after success.
- passed `git diff --check`：No whitespace errors; Git reported LF-to-CRLF warnings only.

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 新项目引导 | 调用 spec_bootstrap 且 projectKind 为 new | 生成 AGENTS.md、specs 基础文档和一个起步 active spec | initialPrompt 缺省时生成项目起步规划提示，要求先确认目标和范围 | 不扫描源码，不生成 review 任务，避免新项目一开始就进入旧系统审查流程 | bun test/smoke.ts | `src/spec/scaffold.ts`<br>`test/smoke.ts` |
| 旧项目引导 | 调用 spec_bootstrap 且 projectKind 为 existing | 生成 AGENTS.md、specs 基础文档和 AI 源码审查任务 | 复用 spec_generate_from_source，review spec 只提供静态线索，要求 AI 阅读源码后总结 | 如果已有文件且 overwrite=false，则保留旧文件并报告 skipped | bun test/smoke.ts | `src/spec/scaffold.ts`<br>`src/templates/spec-documents.ts`<br>`test/smoke.ts` |
| 自动项目类型 | 调用 spec_bootstrap 且 projectKind 为 auto 或未传 | 扫描源码线索，有实现文件时走旧项目流程，否则走新项目流程 | projectKind 默认 auto，maxFiles 默认 800 | 只有 manifest 而无实现文件时按新项目处理 | bun run build; bun test/smoke.ts | `src/spec/scaffold.ts`<br>`src/mcp/register-read-tools.ts` |
| 流程约束 | 模型阅读 spec_context、AGENTS 或 README | 被引导先 bootstrap，再按小任务 TODO、功能 active spec、checkpoint、done 的稳定顺序执行 | spec_done 说明被收紧为只有实现、TODO、验证和最终行为契约完成后才能归档 | checkpoint next step 不再轻率提示可直接 done | rg spec_bootstrap; bun test/smoke.ts | `src/templates/prompt-protocol.ts`<br>`src/templates/agents.ts`<br>`src/spec/checkpoint-writer.ts`<br>`README.md` |

### Risks

- 当前 spec_bootstrap 是 MCP 工具入口，CLI 尚未新增同名命令；如果希望终端也一键初始化，需要后续补 CLI。

### Blockers

- 无

## Done

- doneAt: 2026-06-20T04:58:07.395Z
- note: 已新增统一项目引导入口 spec_bootstrap，并收紧状态流转规则。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 统一项目引导入口 | 用户或模型不知道该创建 TODO、active spec、review 还是 done | 优先调用 spec_bootstrap 生成稳定入口；新项目生成起步 active spec，旧项目生成 AI 源码审查任务 | projectKind 默认为 auto；有实现源码时按 existing，否则按 new | spec_done 只能在实现、TODO、验证和最终行为契约完成后使用 | bun run build; bun test/smoke.ts; bun test/unit.ts; bun run release:check; git diff --check | `src/spec/scaffold.ts`<br>`src/mcp/register-read-tools.ts`<br>`src/templates/prompt-protocol.ts`<br>`src/templates/agents.ts`<br>`src/spec/checkpoint-writer.ts`<br>`README.md`<br>`test/smoke.ts` |
