# 拆分 CLI 子命令代码

## Meta

- status: done
- source: user-prompt

## 用户原始描述

重构 CLI 代码组织：把 src/cli/main.ts 中的通用参数解析、bootstrap 命令、status 命令拆到独立文件，保持 runCli 分发逻辑清晰。目标是提升 AI 可修改和人类可读性，不改变现有 CLI 行为、输出文案或测试契约。要求新文件有顶部文件注释；不引入命令行框架；保持现有 smoke/unit/release-check 通过。

## TODO

- [x] 重构 CLI 代码组织：把 src/cli/main.ts 中的通用参数解析、bootstrap 命令、status 命令拆到独立文件，保持 runCli 分发逻辑清晰。目标是提升 AI 可修改和人类可读性，不改变现有 CLI 行为、输出文案或测试契约。要求新文件有顶部文件注释；不引入命令行框架；保持现有 smoke/unit/release-check 通过。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T15:19:45.084Z
- summary: 拆分 CLI 子命令代码：main.ts 保留分发，bootstrap/status 和通用 option 解析进入独立文件，保持行为不变。

### Summary

- 拆分 CLI 子命令代码：main.ts 保留分发，bootstrap/status 和通用 option 解析进入独立文件，保持行为不变。

### Completed TODOs

- 重构 CLI 代码组织：把 src/cli/main.ts 中的通用参数解析、bootstrap 命令、status 命令拆到独立文件，保持 runCli 分发逻辑清晰。目标是提升 AI 可修改和人类可读性，不改变现有 CLI 行为、输出文案或测试契约。要求新文件有顶部文件注释；不引入命令行框架；保持现有 smoke/unit/release-check 通过。

### Changed Files

- `src/cli/main.ts`
- `src/cli/cli-options.ts`
- `src/cli/command-bootstrap.ts`
- `src/cli/command-status.ts`

### Verification

- passed `bun run build`：TypeScript 构建通过，新增模块 import/export 正常。
- passed `bun test/smoke.ts`：CLI 行为、status/bootstrap、注册 smoke 全部通过。
- passed `bun test/unit.ts`：单元测试通过。
- passed `bun run release:check`：发布契约检查通过。
- passed `node dist/index.js status --project-root . --json`：拆分后 status JSON 行为保持。
- passed `node dist/index.js bootstrap --unknown --help`：拆分后 bootstrap help 优先级保持。
- passed `node dist/index.js status --project-rooot .`：拆分后未知参数仍 Fail Fast。
- passed `git diff --check`：无 whitespace error；仅有 Git LF/CRLF 提示。

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| CLI 主分发 | runCli 收到 status/bootstrap/init/serve/version/help 命令 | main.ts 只负责顶层分发，status/bootstrap 调用独立 command 模块。 | 未传命令仍显示顶层 help。 | 未知顶层命令仍抛 Unknown command。 | bun test/smoke.ts | `src/cli/main.ts` |
| bootstrap 子命令 | 运行 specc bootstrap 或 specc bootstrap --help | 行为和输出保持不变，逻辑迁移到 command-bootstrap.ts。 | projectKind 默认 auto；--help 不写文件。 | 未知参数和非法 projectKind 仍 Fail Fast。 | bun test/smoke.ts; node dist/index.js bootstrap --unknown --help | `src/cli/command-bootstrap.ts`<br>`src/cli/cli-options.ts` |
| status 子命令 | 运行 specc status 或 specc status --json | 行为和输出保持不变，逻辑迁移到 command-status.ts。 | projectRoot 默认当前目录，specsDir 默认 specs。 | openTodos 计数、JSON 输出、未知参数校验保持。 | node dist/index.js status --project-root . --json | `src/cli/command-status.ts`<br>`src/cli/cli-options.ts` |

### Risks

- 无

### Blockers

- 无

## Done

- doneAt: 2026-06-20T15:20:01.713Z
- note: CLI command code split into focused modules without behavior changes.

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| CLI 主分发 | runCli 收到 status/bootstrap/init/serve/version/help 命令 | main.ts 只负责顶层分发，status/bootstrap 调用独立 command 模块。 | 未传命令仍显示顶层 help。 | 未知顶层命令仍抛 Unknown command。 | bun test/smoke.ts | `src/cli/main.ts` |
| bootstrap 子命令 | 运行 specc bootstrap 或 specc bootstrap --help | 行为和输出保持不变，逻辑迁移到 command-bootstrap.ts。 | projectKind 默认 auto；--help 不写文件。 | 未知参数和非法 projectKind 仍 Fail Fast。 | bun test/smoke.ts; node dist/index.js bootstrap --unknown --help | `src/cli/command-bootstrap.ts`<br>`src/cli/cli-options.ts` |
| status 子命令 | 运行 specc status 或 specc status --json | 行为和输出保持不变，逻辑迁移到 command-status.ts。 | projectRoot 默认当前目录，specsDir 默认 specs。 | openTodos 计数、JSON 输出、未知参数校验保持。 | node dist/index.js status --project-root . --json | `src/cli/command-status.ts`<br>`src/cli/cli-options.ts` |
