# 统一 status 工作流推荐来源

## Meta

- status: done
- source: user-prompt

## 用户原始描述

统一 specc status 与 spec_list/spec_context 的工作流推荐来源，减少下一步工具推荐逻辑漂移。要求：
- specc status 的 recommendation 应复用 src/spec/workflow-next-step.ts 的推荐结果或同一核心决策函数，不再维护独立分支规则。
- 保留 specc status --json 的 schemaVersion、nextStep、recommendation.nextTool、alternatives、arguments、reason 公共契约。
- status 文本输出仍保持简洁可读。
- 更新单元测试覆盖 status 推荐来源与典型状态：空项目、只有 done、active/todo/open TODO、只有 review。
- 更新 smoke 测试中受影响的 status JSON 断言。
- 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

## TODO

- [x] 统一 specc status 与 spec_list/spec_context 的工作流推荐来源，减少下一步工具推荐逻辑漂移。要求：
- [x] specc status 的 recommendation 应复用 src/spec/workflow-next-step.ts 的推荐结果或同一核心决策函数，不再维护独立分支规则。
- [x] 保留 specc status --json 的 schemaVersion、nextStep、recommendation.nextTool、alternatives、arguments、reason 公共契约。
- [x] status 文本输出仍保持简洁可读。
- [x] 更新单元测试覆盖 status 推荐来源与典型状态：空项目、只有 done、active/todo/open TODO、只有 review。
- [x] 更新 smoke 测试中受影响的 status JSON 断言。
- [x] 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T16:36:19.341Z
- summary: 已让 specc status 的推荐结果复用 workflow-next-step 的 inspect 阶段核心决策，保留 status JSON 公共契约，并补充 review-only 与 open TODO 分支覆盖。

### Summary

- 已让 specc status 的推荐结果复用 workflow-next-step 的 inspect 阶段核心决策，保留 status JSON 公共契约，并补充 review-only 与 open TODO 分支覆盖。

### Completed TODOs

- 统一 specc status 与 spec_list/spec_context 的工作流推荐来源，减少下一步工具推荐逻辑漂移。要求：
- specc status 的 recommendation 应复用 src/spec/workflow-next-step.ts 的推荐结果或同一核心决策函数，不再维护独立分支规则。
- 保留 specc status --json 的 schemaVersion、nextStep、recommendation.nextTool、alternatives、arguments、reason 公共契约。
- status 文本输出仍保持简洁可读。
- 更新单元测试覆盖 status 推荐来源与典型状态：空项目、只有 done、active/todo/open TODO、只有 review。
- 更新 smoke 测试中受影响的 status JSON 断言。
- 验证 bun run build、bun test/unit.ts、bun test/smoke.ts、bun run release:check、git diff --check。

### Changed Files

- `src/cli/status-recommendation.ts`
- `src/spec/workflow-next-step.ts`
- `test/unit.ts`

### Verification

- passed `bun run build`：TypeScript 构建通过。
- passed `bun test/unit.ts`：覆盖 empty、done-only、active、review-only、open TODO 推荐。
- passed `bun test/smoke.ts`：端到端 status JSON 契约仍通过。
- passed `bun run release:check`：发布契约检查通过。
- passed `git diff --check`：退出码为 0，仅出现 Windows LF/CRLF 提示。

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| status 推荐来源 | specc status 需要生成 recommendation | status-recommendation.ts 将计数状态适配成 workflow-next-step 的 WorkflowState，并调用 recommendedWorkflowStep(..., "inspect") 生成 nextTool、alternatives、arguments、reason。 | status 仍输出 schemaVersion 1，并保留旧 nextStep 字符串作为兼容字段。 | 未记录 | bun run build && bun test/unit.ts | `src/cli/status-recommendation.ts`<br>`src/spec/workflow-next-step.ts` |
| inspect 阶段 open TODO 优先 | 存在 open TODO 时查看 spec_list 或 specc status | 共享推荐优先返回 spec_context，要求先读取本轮上下文，再按 TODO 顺序执行。 | 未记录 | open TODO 优先级高于 active/todo 普通推荐。 | bun test/unit.ts && bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`src/cli/status-recommendation.ts` |
| review-only 推荐 | 只有 review spec，没有 active/todo/open TODO | status 复用共享推荐，返回 spec_context，并携带 files 占位参数指向 review spec。 | 未记录 | 未记录 | bun test/unit.ts | `src/cli/status-recommendation.ts`<br>`test/unit.ts` |
| 公共 JSON 契约 | 调用 specc status --json | 继续输出 schemaVersion、nextStep、recommendation.nextTool、alternatives、arguments、reason；文本输出仍使用简洁 Next Step 行。 | 未记录 | 未记录 | bun test/smoke.ts | `src/cli/command-status.ts`<br>`src/cli/status-recommendation.ts`<br>`test/smoke.ts` |

### Risks

- 无

### Blockers

- 无

## Done

- doneAt: 2026-06-20T16:36:44.840Z
- note: status 工作流推荐来源统一完成并通过验收。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| status 推荐来源 | specc status 需要生成 recommendation | status-recommendation.ts 将计数状态适配成 workflow-next-step 的 WorkflowState，并调用 recommendedWorkflowStep(..., "inspect") 生成 nextTool、alternatives、arguments、reason。 | status 仍输出 schemaVersion 1，并保留旧 nextStep 字符串作为兼容字段。 | 未记录 | bun run build && bun test/unit.ts | `src/cli/status-recommendation.ts`<br>`src/spec/workflow-next-step.ts` |
| inspect 阶段 open TODO 优先 | 存在 open TODO 时查看 spec_list 或 specc status | 共享推荐优先返回 spec_context，要求先读取本轮上下文，再按 TODO 顺序执行。 | 未记录 | open TODO 优先级高于 active/todo 普通推荐。 | bun test/unit.ts && bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`src/cli/status-recommendation.ts` |
| review-only 推荐 | 只有 review spec，没有 active/todo/open TODO | status 复用共享推荐，返回 spec_context，并携带 files 占位参数指向 review spec。 | 未记录 | 未记录 | bun test/unit.ts | `src/cli/status-recommendation.ts`<br>`test/unit.ts` |
| 公共 JSON 契约 | 调用 specc status --json | 继续输出 schemaVersion、nextStep、recommendation.nextTool、alternatives、arguments、reason；文本输出仍使用简洁 Next Step 行。 | 未记录 | 未记录 | bun test/smoke.ts | `src/cli/command-status.ts`<br>`src/cli/status-recommendation.ts`<br>`test/smoke.ts` |
