# 源码反推 spec 改为 AI 指导式流程

## Meta

- status: done
- source: user-prompt

## 用户原始描述

调整 spec_generate_from_source：MCP 不应自己根据文件名或浅层扫描生成业务 spec 结论，而应生成指导 AI 阅读源码的 review/active 草稿。工具只提供候选模块、证据路径、搜索提示、待确认问题和输出模板；业务行为、分支条件、默认参数和边界处理必须由 AI 阅读源码后总结。更新实现、模板和测试，确保反推源码功能偏指导性，不替代 AI 的理解能力。

## TODO

- [x] 调整 spec_generate_from_source：MCP 不应自己根据文件名或浅层扫描生成业务 spec 结论，而应生成指导 AI 阅读源码的 review/active 草稿。工具只提供候选模块、证据路径、搜索提示、待确认问题和输出模板；业务行为、分支条件、默认参数和边界处理必须由 AI 阅读源码后总结。更新实现、模板和测试，确保反推源码功能偏指导性，不替代 AI 的理解能力。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T04:51:51.822Z
- summary: 将 spec_generate_from_source 改为生成 AI 源码审查任务，不再把静态扫描结果描述为业务事实或当前代码结论。

### Summary

- 将 spec_generate_from_source 改为生成 AI 源码审查任务，不再把静态扫描结果描述为业务事实或当前代码结论。

### Completed TODOs

- 调整 spec_generate_from_source：MCP 不应自己根据文件名或浅层扫描生成业务 spec 结论，而应生成指导 AI 阅读源码的 review/active 草稿。工具只提供候选模块、证据路径、搜索提示、待确认问题和输出模板；业务行为、分支条件、默认参数和边界处理必须由 AI 阅读源码后总结。更新实现、模板和测试，确保反推源码功能偏指导性，不替代 AI 的理解能力。

### Changed Files

- `src/templates/spec-documents.ts`
- `src/spec/source-scan.ts`
- `src/spec/scaffold.ts`
- `src/mcp/register-read-tools.ts`
- `src/templates/agents.ts`
- `README.md`
- `test/smoke.ts`

### Verification

- passed `bun run build`：TypeScript build passed.
- passed `bun test/smoke.ts`：Smoke test passed; Bun printed sandbox EPERM warning after success.
- passed `bun test/unit.ts`：Unit tests passed; Bun printed sandbox EPERM warning after success.
- passed `bun run release:check`：Release checks passed; Bun printed sandbox EPERM warning after success.
- passed `git diff --check`：No whitespace errors; Git reported LF-to-CRLF warnings only.
- passed `rg "source-derived/current-code|反推出此能力|大概率存在相关实现|当前代码似乎|源码反推的当前代码事实" src test README.md -n`：No active old wording remains.

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 生成源码审查任务 | 调用 spec_generate_from_source 并扫描到源码线索 | 生成 review 文件，状态为 source-review/needs-ai-summary，明确说明静态线索不是业务事实，AI 必须阅读源码后总结 | 仍生成 source-inventory、index 和候选 review 文件，但内容改为阅读任务、搜索线索、待确认问题和输出模板 | 未识别到路由、组件、模型或测试时保留“未识别到...”占位，不推断业务结论 | bun test/smoke.ts | `src/templates/spec-documents.ts`<br>`src/spec/source-scan.ts`<br>`src/spec/scaffold.ts` |
| MCP 工具说明 | 模型查看 spec_generate_from_source 工具描述或返回标题 | 描述为生成 AI source-review tasks，要求模型自行读取源码后再总结业务行为 | 工具继续接受 projectRoot、includePatterns、excludePatterns、maxFiles 等扫描参数 | 扫描结果只作为搜索入口，不作为事实来源 | bun run build; bun test/smoke.ts | `src/mcp/register-read-tools.ts` |
| 文档和 AGENTS 约束 | 用户或模型阅读 README/AGENTS 模板中的 review 目录说明 | 说明 review 是指导 AI 阅读源码并总结真实行为的任务，不再称为当前代码事实 | 新生成 AGENTS.md 会包含 source-review/needs-ai-summary 状态说明 | 旧状态 source-derived/current-code 的主动文档说明已移除 | rg old wording; bun test/smoke.ts | `README.md`<br>`src/templates/agents.ts` |

### Risks

- 历史 specs/done 文档可能仍记录旧语义，未在本次任务中迁移。

### Blockers

- 无

## Done

- doneAt: 2026-06-20T04:52:01.906Z
- note: spec_generate_from_source 已改为 AI 源码审查任务生成器。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 源码审查任务生成 | 用户调用 spec_generate_from_source | MCP 只生成静态线索、阅读入口、待确认问题和 AI 总结模板，不输出业务结论 | 状态使用 source-review/needs-ai-summary；业务目标、分支、默认值和边界处理均要求 AI 阅读源码后填写 | 缺少某类线索时写“未识别到...”，不自行补全业务推断 | bun run build; bun test/smoke.ts; bun test/unit.ts; bun run release:check; git diff --check | `src/templates/spec-documents.ts`<br>`src/spec/source-scan.ts`<br>`src/spec/scaffold.ts`<br>`src/mcp/register-read-tools.ts`<br>`src/templates/agents.ts`<br>`README.md`<br>`test/smoke.ts` |
