# 审计并精简 MCP 工具集

## Meta

- status: done
- source: user-prompt

## 用户原始描述

审计 Spec Coding MCP 的工具集是否存在重复、不够完美或入口过多的问题。围绕 spec_bootstrap 作为统一入口，检查 spec_init、spec_generate_agents、spec_generate_from_source、spec_create、spec_todo、spec_checkpoint、spec_review_result、spec_done 等工具边界。优化或删除重复工具；如果为了高级场景保留工具，需要在描述中明确它们是高级/底层入口，并在 README、AGENTS、测试中体现推荐主路径。目标是减少模型困惑，让工具集更专业、一致、可预期。

## TODO

- [x] 审计 Spec Coding MCP 的工具集是否存在重复、不够完美或入口过多的问题。围绕 spec_bootstrap 作为统一入口，检查 spec_init、spec_generate_agents、spec_generate_from_source、spec_create、spec_todo、spec_checkpoint、spec_review_result、spec_done 等工具边界。优化或删除重复工具；如果为了高级场景保留工具，需要在描述中明确它们是高级/底层入口，并在 README、AGENTS、测试中体现推荐主路径。目标是减少模型困惑，让工具集更专业、一致、可预期。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T05:03:53.673Z
- summary: 审计 MCP 工具集后保留底层工具，但按主路径、任务创建、结果记录、高级维护重新分层，并强化工具描述和 README。

### Summary

- 审计 MCP 工具集后保留底层工具，但按主路径、任务创建、结果记录、高级维护重新分层，并强化工具描述和 README。

### Completed TODOs

- 审计 Spec Coding MCP 的工具集是否存在重复、不够完美或入口过多的问题。围绕 spec_bootstrap 作为统一入口，检查 spec_init、spec_generate_agents、spec_generate_from_source、spec_create、spec_todo、spec_checkpoint、spec_review_result、spec_done 等工具边界。优化或删除重复工具；如果为了高级场景保留工具，需要在描述中明确它们是高级/底层入口，并在 README、AGENTS、测试中体现推荐主路径。目标是减少模型困惑，让工具集更专业、一致、可预期。

### Changed Files

- `src/mcp/register-read-tools.ts`
- `src/mcp/register-write-tools.ts`
- `README.md`
- `test/smoke.ts`

### Verification

- passed `bun run build`：TypeScript build passed.
- passed `bun test/smoke.ts`：Smoke test passed; Bun printed sandbox EPERM warning after success.
- passed `bun test/unit.ts`：Unit tests passed; Bun printed sandbox EPERM warning after success.
- passed `bun run release:check`：Release checks passed; Bun printed sandbox EPERM warning after success.
- passed `git diff --check`：No whitespace errors; Git reported LF-to-CRLF warnings only.

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 主入口选择 | 模型或用户需要初始化新项目或旧项目 | 工具描述明确 spec_bootstrap 是 PRIMARY ENTRYPOINT，优先于 spec_init、spec_generate_agents、spec_generate_from_source | spec_bootstrap 仍默认 auto 判断新/旧项目 | 需要只刷新模板、AGENTS 或源码审查任务时，仍可使用高级工具 | bun test/smoke.ts | `src/mcp/register-read-tools.ts`<br>`test/smoke.ts` |
| 高级工具保留 | 用户只想执行局部维护动作 | spec_init、spec_generate_agents、spec_generate_from_source 被保留，但描述为 Advanced helper，并提示普通接入优先使用 spec_bootstrap | 不破坏已有工具名称和 MCP 调用方式 | 避免删除工具导致已有配置或用户习惯中断 | bun test/smoke.ts | `src/mcp/register-read-tools.ts`<br>`src/mcp/register-write-tools.ts` |
| 结果记录边界 | 模型完成部分或全部实现后选择记录工具 | spec_checkpoint 用于阶段记录；spec_review_result 用于高级交接/部分完成；spec_done 只用于完全实现并验证的归档 | 所有写工具仍要求先调用 spec_context | spec_done 描述明确 Do not use for partial work | bun test/smoke.ts | `src/mcp/register-write-tools.ts` |
| README 工具面 | 用户阅读 MCP 工具说明 | 工具按主路径、任务创建、结果记录、高级维护分组，不再平铺造成误选 | 推荐路径从 spec_bootstrap 到 spec_context，再按任务类型选择 spec_create/spec_todo | 高级维护工具说明保留但不作为推荐入口 | git diff --check | `README.md` |

### Risks

- 工具数量没有减少，但通过分层和描述降低误用；如果后续仍觉得多，可以再增加精简模式只暴露主路径工具。

### Blockers

- 无

## Done

- doneAt: 2026-06-20T05:04:06.547Z
- note: 已完成 MCP 工具集分层和主路径收敛。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 工具集分层 | 用户或模型选择 MCP 工具 | spec_bootstrap 是主入口；spec_context 是写操作前置；spec_create/spec_todo 创建任务；spec_checkpoint/spec_review_result/spec_done 记录结果；spec_init/spec_generate_agents/spec_generate_from_source 是高级维护工具 | 不删除现有工具，避免破坏已有使用方式；通过描述和 README 引导主路径 | spec_done 明确禁止用于 partial work；高级工具描述均提示优先使用 spec_bootstrap | bun run build; bun test/smoke.ts; bun test/unit.ts; bun run release:check; git diff --check | `src/mcp/register-read-tools.ts`<br>`src/mcp/register-write-tools.ts`<br>`README.md`<br>`test/smoke.ts` |
