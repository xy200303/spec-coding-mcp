# 推荐参数使用真实上下文值

## Meta

- status: done
- source: user-prompt

## 用户原始描述

优化 Recommended Next Step 的 arguments：能从当前上下文安全确定的参数使用真实值，例如 projectRoot、specsDir、selected spec file、当前 TODO 文件；无法安全确定的业务内容继续使用占位说明。不要虚构 prompt、title、behaviorRecords。更新 workflow-next-step 设计、spec_context/spec_list 调用、README 和 smoke 测试。

## TODO

- [x] 优化 Recommended Next Step 的 arguments：能从当前上下文安全确定的参数使用真实值，例如 projectRoot、specsDir、selected spec file、当前 TODO 文件；无法安全确定的业务内容继续使用占位说明。不要虚构 prompt、title、behaviorRecords。更新 workflow-next-step 设计、spec_context/spec_list 调用、README 和 smoke 测试。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T07:13:32.106Z
- summary: 将 Recommended Next Step 的 arguments 从通用占位升级为优先使用当前上下文可确定的真实值。

### Summary

- 将 Recommended Next Step 的 arguments 从通用占位升级为优先使用当前上下文可确定的真实值。

### Completed TODOs

- 优化 Recommended Next Step 的 arguments：能从当前上下文安全确定的参数使用真实值，例如 projectRoot、specsDir、selected spec file、当前 TODO 文件；无法安全确定的业务内容继续使用占位说明。不要虚构 prompt、title、behaviorRecords。更新 workflow-next-step 设计、spec_context/spec_list 调用、README 和 smoke 测试。

### Changed Files

- `src/spec/workflow-next-step.ts`
- `src/spec/context-markdown.ts`
- `src/mcp/register-read-tools.ts`
- `README.md`
- `test/smoke.ts`

### Verification

- passed `bun run build`：TypeScript 构建通过。
- passed `bun test/smoke.ts`：smoke 测试通过；覆盖真实 projectRoot/specsDir/file 参数输出；Bun 末尾输出已知 EPERM 警告但退出码为 0。
- passed `bun test/unit.ts`：unit 测试通过。
- passed `bun run release:check`：发布契约检查通过；Bun 末尾输出已知 EPERM 警告但退出码为 0。
- passed `git diff --check`：无 whitespace error；仅有 Git LF/CRLF 提示。

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 可确定参数真实化 | workflowRecommendationLines 接收到 projectRoot、specsDir 和当前工作项状态 | arguments 使用真实 projectRoot、specsDir，并在 checkpoint 推荐中使用当前 open TODO 或 selected/active spec 文件。 | projectRoot 和 specsDir 始终来自调用上下文；当前文件优先使用第一个 open TODO 所在文件。 | 没有可确定文件时继续使用占位说明，不虚构文件路径。 | bun run build; bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`src/spec/context-markdown.ts`<br>`src/mcp/register-read-tools.ts`<br>`test/smoke.ts` |
| 业务内容保持占位 | 推荐 spec_create、spec_todo 或 spec_checkpoint 时需要 prompt、title、completedTodos、verification、behaviorRecords | 业务内容仍保留占位说明，避免模型编造需求、标题或行为记录。 | 只填入上下文可安全确定的工具参数。 | review spec 只把已存在 review 文件作为 files 参数，不把静态线索当业务事实。 | bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`test/smoke.ts` |
| 文档说明 | 用户阅读 README 的 Recommended Next Step 说明 | README 明确 arguments 会优先填真实上下文值，无法确定的业务内容继续用占位。 | 模型优先执行 nextTool，alternatives 仍只是备选。 | 不要求用户维护额外参数映射。 | git diff --check | `README.md` |

### Risks

- 无

### Blockers

- 无

## Done

- doneAt: 2026-06-20T07:13:45.277Z
- note: Recommended Next Step arguments now use safe real context values when available.

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 可确定参数真实化 | workflowRecommendationLines 接收到 projectRoot、specsDir 和当前工作项状态 | arguments 使用真实 projectRoot、specsDir，并在 checkpoint 推荐中使用当前 open TODO 或 selected/active spec 文件。 | projectRoot 和 specsDir 始终来自调用上下文；当前文件优先使用第一个 open TODO 所在文件。 | 没有可确定文件时继续使用占位说明，不虚构文件路径。 | bun run build; bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`src/spec/context-markdown.ts`<br>`src/mcp/register-read-tools.ts`<br>`test/smoke.ts` |
| 业务内容保持占位 | 推荐 spec_create、spec_todo 或 spec_checkpoint 时需要 prompt、title、completedTodos、verification、behaviorRecords | 业务内容仍保留占位说明，避免模型编造需求、标题或行为记录。 | 只填入上下文可安全确定的工具参数。 | review spec 只把已存在 review 文件作为 files 参数，不把静态线索当业务事实。 | bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`test/smoke.ts` |
| 文档说明 | 用户阅读 README 的 Recommended Next Step 说明 | README 明确 arguments 会优先填真实上下文值，无法确定的业务内容继续用占位。 | 模型优先执行 nextTool，alternatives 仍只是备选。 | 不要求用户维护额外参数映射。 | git diff --check | `README.md` |
