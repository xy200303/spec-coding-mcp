# 让 nextTool 始终是单一工具

## Meta

- status: done
- source: user-prompt

## 用户原始描述

继续优化结构化 Recommended Next Step：nextTool 字段必须始终是单一工具 ID，不能出现 spec_todo/spec_create 这种二选一字符串。需要增加 candidates 或 alternatives 字段承载可选工具，并调整渲染、README 和测试。目标是让模型更稳定地执行 nextTool，同时还能看到备选项。

## TODO

- [x] 继续优化结构化 Recommended Next Step：nextTool 字段必须始终是单一工具 ID，不能出现 spec_todo/spec_create 这种二选一字符串。需要增加 candidates 或 alternatives 字段承载可选工具，并调整渲染、README 和测试。目标是让模型更稳定地执行 nextTool，同时还能看到备选项。

## 执行要求

- 开始前必须先调用 `spec_context`，确认当前 TODO 上下文和工程约束。
- AI 必须按未勾选 TODO 从上到下执行。
- 完成任务后把对应项改成 `[x]`。
- 无法完成的任务保持 `[ ]`，并在任务下方写明阻塞原因。
- 完成后必须记录实际行为：业务分支条件、默认参数行为、边界处理结果和验证结果。

## 实际行为记录

- 分支条件：完成后补充已实现行为。
- 默认参数行为：完成后补充默认值和覆盖规则。
- 边界处理结果：完成后补充异常、空值、权限、状态等处理结果。
- 验证结果：完成后记录验证命令和结果。

## 工程质量约束

这些规则是强制约束，不是建议。

### Hard Rules

- Fail Fast：尽早校验输入、依赖、前置条件和无效状态。
- 风险先确认：不明确、高影响或高风险决策先问用户。
- 文件注释：新建或重写文件保留顶部注释；复杂边界写为什么，不写废话。
- 禁止在一个文件里混合 UI、业务、数据访问逻辑；禁止在领域层引用 Web / DB 框架。
- 禁止为了模式而模式：不要无故引入接口、工厂、泛型、抽象层。
- 性能与资源：避免不必要高复杂度，不阻塞主线程，不泄露连接、内存或文件句柄。

### Recommended Practices

- KISS + YAGNI：优先最简单可用方案，不预埋未确认复杂度。
- Clean Code：业务意图命名，短函数，低嵌套，DRY，显式行为。
- Human Readable：按线性故事写代码，复杂逻辑拆成有语义的小步骤。
- Clean Architecture + DDD：按业务能力分层，领域规则不依赖框架、DB 或 Web。
- SOLID + SoC：职责单一，关注点分离，组合优于继承，依赖抽象。
- 测试优先：核心逻辑可单测，验证命令和结果必须记录。
- 向后兼容：小步修改，不破坏已有 API、数据和行为契约。
- 成熟库优先：已有成熟方案不手搓；新增依赖先确认必要性。
- 项目结构：按业务语义拆分目录和文件，避免单文件堆砌和目录平铺。
- UI/交互：符合直觉，状态完整，文案简洁，布局清楚。
- Boy Scout Rule：局部顺手清理，不做无关大重构。
- AI + Human：结构清晰、边界明确，便于 AI 修改和人类维护。

## 业务不确定性强制确认

这些规则是硬性约束，不是建议。

- 业务不确定性强制确认：金额、费率、结算、退款、折扣、税费、状态机、并发、幂等、重试、回滚、规则来源不明或角色差异，必须先问清楚。
- 禁止猜业务：不要用常识补规则，不要自行假设边界。
- 澄清格式：说明不清楚之处，给出 2 到 3 种可能解释，等待用户确认。
- 金钱与合规：涉及钱、合规、审计的实现必须有明确来源或产品确认注释。

## Checkpoint

- at: 2026-06-20T06:01:17.505Z
- summary: 确保 Recommended Next Step 的 nextTool 始终是单一工具 ID，并新增 alternatives 字段承载备选工具。

### Summary

- 确保 Recommended Next Step 的 nextTool 始终是单一工具 ID，并新增 alternatives 字段承载备选工具。

### Completed TODOs

- 继续优化结构化 Recommended Next Step：nextTool 字段必须始终是单一工具 ID，不能出现 spec_todo/spec_create 这种二选一字符串。需要增加 candidates 或 alternatives 字段承载可选工具，并调整渲染、README 和测试。目标是让模型更稳定地执行 nextTool，同时还能看到备选项。

### Changed Files

- `src/spec/workflow-next-step.ts`
- `README.md`
- `test/smoke.ts`

### Verification

- passed `bun run build`：TypeScript build passed.
- passed `bun test/smoke.ts`：Smoke test passed; Bun printed sandbox EPERM warning after success.
- passed `bun test/unit.ts`：Unit tests passed; Bun printed sandbox EPERM warning after success.
- passed `bun run release:check`：Release checks passed.
- passed `git diff --check`：No whitespace errors; Git reported LF-to-CRLF warnings only.
- passed `rg slash-combined nextTool`：No slash-combined nextTool output remains.

### 实际行为记录

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 单一 nextTool | Recommended Next Step 渲染工具推荐 | nextTool 始终是单一工具 ID | 无备选时 alternatives 显示“无” | 需要表达 spec_todo/spec_create 选择时，nextTool 使用 spec_todo，alternatives 使用 spec_create | rg slash-combined nextTool; bun test/smoke.ts | `src/spec/workflow-next-step.ts`<br>`test/smoke.ts` |
| 备选工具提示 | 推荐存在可选工具 | alternatives 字段列出备选工具，模型优先执行 nextTool | README 明确只有用户要求或条件不满足时才考虑 alternatives | active spec 阶段 nextTool 为 spec_checkpoint，alternatives 可提示 spec_done 但仍要求验证和最终行为契约 | bun run build | `README.md`<br>`src/spec/workflow-next-step.ts` |

### Risks

- 无

### Blockers

- 无

## Done

- doneAt: 2026-06-20T06:01:28.663Z
- note: Recommended Next Step 的 nextTool 已单值化，备选工具进入 alternatives。

## 最终行为契约

| 场景 | 条件 | 结果 | 默认行为 | 边界处理 | 验证 | 关联文件 |
|---|---|---|---|---|---|---|
| 结构化工具选择 | Recommended Next Step 输出工具建议 | nextTool 始终是单一工具 ID，alternatives 承载备选工具 | 无备选时 alternatives 为“无” | 不再输出 spec_todo/spec_create 这类组合工具名 | bun run build; bun test/smoke.ts; bun test/unit.ts; bun run release:check; git diff --check; rg slash-combined nextTool | `src/spec/workflow-next-step.ts`<br>`README.md`<br>`test/smoke.ts` |
